'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.parseString = parseString;

var _lexer = require('./lexer/lexer');

var _lexer2 = _interopRequireDefault(_lexer);

var _Bibliography = require('./../bibliography/Bibliography');

var _Bibliography2 = _interopRequireDefault(_Bibliography);

var _parser = require('./parser/parser');

var _parser2 = _interopRequireDefault(_parser);

var _nearley = require('nearley');

var _nearley2 = _interopRequireDefault(_nearley);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * @param str Bibliography file that should conform to BibTex standard
 * @return Bibliography the parsed bibliography
 */
function parseString(str) {
  // Tokenize string
  let tokens = [];
  const lexer = new _lexer2.default(str);
  let nextToken;
  while (nextToken = lexer.readNextToken()) tokens.push(nextToken);

  // Parse tokens
  const parser = new _nearley2.default.Parser(_parser2.default.ParserRules, _parser2.default.ParserStart);
  parser.feed(tokens);
  const result = parser.results;

  if (result.length <= 0) throw new Error("Found 0 parses for input string.");else if (result.length > 1) console.warn("Found multiple parses for input string. This is a bug. Please report this issue to https://github.com/digitalheir/bibliography-js/issues");
  return new _Bibliography2.default(result[0]);
}